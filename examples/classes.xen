include io;

class Counter {
    private count = 0;

    private fn inc() {
        this.count = this.count + 1;
    }

    fn increment() {
        this.inc();
    }

    fn get() => this.count
};

var c = new Counter();
c.increment();
c.increment();
io.println(c.get());
